import { ChipAddOnBlurDirective } from './chip-add-on-blur.directive';

describe('ChipAddOnBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipAddOnBlurDirective(null);
    expect(directive).toBeTruthy();
  });
});
