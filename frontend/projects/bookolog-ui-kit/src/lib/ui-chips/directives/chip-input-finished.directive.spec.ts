import { ChipInputFinishedDirective } from './chip-input-finished.directive';

describe('ChipInputFinishedDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipInputFinishedDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
